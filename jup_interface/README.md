# stakedex_jup_interface

## Notes

This crate was originally split from `stakedex_sdk_common` in order to avoid cyclic dependencies from having to import `stakedex_unstake_it` for `PrefundSwapViaStake`
